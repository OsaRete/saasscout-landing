import {body} from "@/lib/validation/server/http";import {publicSurveyHandler,publicSurveyLimit} from "@/lib/validation/server/public-survey-http";export const runtime="nodejs";export async function POST(request:Request,{params}:{params:Promise<{token:string}>}){const{token}=await params;return publicSurveyHandler(async service=>{publicSurveyLimit(request,token);return service.submit(token,await body(request))})}
