import {validationHandler} from "@/lib/validation/server/http";import {SurveyService} from "@/lib/validation/server/surveys";import {createSupabaseAdminClient} from "@/lib/supabase/server-admin";export const runtime="nodejs";export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;return validationHandler(request,async(_,ownerId)=>new SurveyService(createSupabaseAdminClient()).revoke(ownerId,id))}
